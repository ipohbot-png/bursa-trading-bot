# Python
__pycache__/
*.pyc

# Data caches (regenerated by data_audit / scanners)
.cache/

# Scanner runtime state (machine-local, not source)
active_signal_state.json
active_signal_log.jsonl
.last_heartbeat.json
paper_portfolio.json

# Bulk backtest outputs — regenerate with `python active_backtest.py`.
# Keep only the report and the key result JSONs under version control.
active_results/*
!active_results/report.html
!active_results/report_template.html
!active_results/promoted_rules.json
!active_results/summary.json
!active_results/data_audit.json
momentum_results/

# Local scratch
*.bak
files.zip
